apiVersion: v1
kind: Service
metadata:
  name: backend-api-service
spec:
  selector:
    app: backend-api
  ports:
  - protocol: TCP
    port: 80          # Port externe du service
    targetPort: 5000  # Port du container
  type: LoadBalancer  # Minikube va créer un tunnel local
